# GPS via gpsd shared memory
refclock SHM 0 refid GPS precision 1e-2 offset 0 delay 0.1 filter 5 prefer

# Internet NTP as fallback
pool pool.ntp.org iburst

driftfile /var/lib/chrony/drift
makestep 1 3
rtcsync

allow 192.168.50.0/24
local stratum 3
