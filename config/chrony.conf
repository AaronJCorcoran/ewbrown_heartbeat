# GPS via gpsd shared memory
refclock SHM 0 refid GPS precision 1e-1 offset 0.2 delay 0.2

# Internet NTP as fallback
pool pool.ntp.org iburst

driftfile /var/lib/chrony/drift
makestep 1 3
rtcsync

allow 192.168.50.0/24
local stratum 3
